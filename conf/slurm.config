/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    daisybio/domainsplit — slurm executor profile
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Picked up via `-profile slurm`. Pair with `singularity` on the cluster
    (the head node norm.exbio has no docker group; singularity user-NS works).
----------------------------------------------------------------------------*/

workDir = '/nfs/scratch/cobinet/work'

// Allow re-runs to overwrite stale execution_*.html / trace files instead of
// aborting with FileAlreadyExistsException.
report   { overwrite = true }
timeline { overwrite = true }
trace    { overwrite = true }
dag      { overwrite = true }

executor {
    name                = 'slurm'
    queueSize           = 20
    submitRateLimit     = '10/1min'
    pollInterval        = '30s'
}

process {
    executor      = 'slurm'
    cpus          = { 2     * task.attempt }
    memory        = { 12.GB * task.attempt }
    time          = { 4.h   * task.attempt }

    // Retry on transient slurm/oom/socket errors AND on docker/pull/network exit codes
    errorStrategy = { task.exitStatus in [104, 126, 127, 137, 139, 140, 143, 247] ? 'retry' : 'finish' }
    maxRetries    = 3

    withLabel: 'process_low' {
        cpus   = { 2     * task.attempt }
        memory = { 12.GB * task.attempt }
        time   = { 4.h   * task.attempt }
    }
    withLabel: 'process_medium' {
        cpus   = { 6     * task.attempt }
        memory = { 36.GB * task.attempt }
        time   = { 8.h   * task.attempt }
    }
    withLabel: 'process_high' {
        cpus   = { 12    * task.attempt }
        memory = { 72.GB * task.attempt }
        time   = { 16.h  * task.attempt }
    }
    withLabel: 'process_long' {
        time   = { 20.h  * task.attempt }
    }
    withLabel: 'process_high_memory' {
        memory = { 200.GB * task.attempt }
    }

    // ------- GPU -------
    // ESM embeddings: large model + long runtime. Same nodelist as
    // sibling domainbenchmark pipeline. compms-gpu-1 excluded (ECC issues).
    withLabel: 'process_gpu_large' {
        queue          = 'shared-gpu'
        cpus           = { 4      * task.attempt }
        memory         = { 60.GB  * task.attempt }
        time           = { 24.h   * task.attempt }
        clusterOptions = '--gpus-per-task=1 --ntasks=1 --qos=limitgpus --nodelist=jlab-gpu01.exbio.wzw.tum.de,gpu02.exbio.wzw.tum.de,compms-gpu-2.exbio.wzw.tum.de'
        maxForks       = 6
    }
    withLabel: 'process_gpu_small' {
        queue          = 'shared-gpu'
        cpus           = { 2      * task.attempt }
        memory         = { 20.GB  * task.attempt }
        time           = { 12.h   * task.attempt }
        clusterOptions = '--gpus-per-task=1 --ntasks=1 --qos=limitgpus --nodelist=jlab-gpu01.exbio.wzw.tum.de,gpu02.exbio.wzw.tum.de,compms-gpu-2.exbio.wzw.tum.de'
        maxForks       = 6
    }
    withLabel: 'process_gpu' {
        queue          = 'shared-gpu'
        cpus           = { 2      * task.attempt }
        memory         = { 20.GB  * task.attempt }
        time           = { 12.h   * task.attempt }
        clusterOptions = '--gpus-per-task=1 --ntasks=1 --qos=limitgpus --nodelist=jlab-gpu01.exbio.wzw.tum.de,gpu02.exbio.wzw.tum.de,compms-gpu-2.exbio.wzw.tum.de'
        maxForks       = 6
    }

    // ------- per-process overrides -------
    // Many small downloads (one per Pfam family). Keep concurrency low to
    // be nice to InterPro and the slurm scheduler. Retry on transient HTTP,
    // then ignore — a handful of obsolete Pfam IDs return 404/500 and we
    // must not abort the whole pipeline over them.
    withName: 'DAISYBIO_DOMAINSPLIT:DOMAINSPLIT:CREATE_COBINET_DATABASE:DOWNLOAD_PFAM_ALIGNMENT' {
        cpus          = 1
        memory        = 2.GB
        time          = 30.min
        maxForks      = 100
        maxRetries    = 3
        errorStrategy = { task.attempt <= 3 ? 'retry' : 'ignore' }
    }

    // Big single-shot UniProt-SwissProt per-residue HDF5 from EBI (~12 GB).
    // 4h is too tight when EBI is slow; bump to 12h and retry once.
    // If this still fails the whole pipeline cannot proceed (downstream
    // COBINET needs the embeddings file), so do NOT ignore here.
    withName: 'DAISYBIO_DOMAINSPLIT:DOMAINSPLIT:CREATE_COBINET_DATABASE:DOWNLOAD_PROTT5_EMBEDDINGS_COMPLETE' {
        cpus          = 2
        memory        = 4.GB
        time          = { 12.h * task.attempt }
        maxRetries    = 2
        errorStrategy = { task.exitStatus in [104, 137, 139, 140, 143, 247] ? 'retry' : 'finish' }
    }
}

singularity {
    enabled    = true
    autoMounts = true
    cacheDir   = '/nfs/scratch/singularity_cache'
    runOptions = '--nv'
    envWhitelist = 'HF_TOKEN,HF_HOME,HUGGINGFACE_HUB_CACHE'
}

conda {
    enabled = false
}

docker {
    enabled = false
}
